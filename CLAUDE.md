# 自治会集金アプリ 引継ぎメモ (CLAUDE.md)

このファイルはプロジェクトルート直下（`hinode2gou/jichikai-app` リポジトリのトップレベル）に配置してください。
Claude Codeがこのディレクトリで起動した際に自動的に読み込まれます。

## プロジェクト概要

自治会（103世帯・マンション）向けの自作集金管理Webアプリ。
- 構成: GitHub Pages（フロントエンド） + GAS（バックエンド） + Googleスプレッドシート（DB）
- リポジトリ: `hinode2gou/jichikai-app`
- 別アカウント（旧Claude環境）から本アカウントに引継ぎ作業中

## 機能追加履歴

### 会計報告書に「出納帳」シートを自動生成＋繰越金の自動提案（2026-09-26）

**経緯:** 財務要約レポート機能（従来「戦略未定」だった残課題）について、旧環境で使っていた手作業の会計シート（`202510-202603会計.xlsx`、シート「出納帳」＋「会計報告」の2枚構成）をユーザーと一緒に確認し、方向性を決めた。「出納帳」シートは日付順・残高積み上げ形式の取引ログ＋現金の金種表（実カウント）で、「なんか抜けている」ことに気づく手がかりになっていた。

**実装内容:**
- `gas/コード.js` の `createAccountingReport`: 会計報告書のスプレッドシートに、従来の「会計報告」シートに加えて**「出納帳」シートを自動生成**するよう拡張。`収支記録`のその期の行を日付順に展開し、入金（自治会費/集会所使用料/滞納回収/来期分/その他収入）・出金（公共料金/清掃費/雑費）を列ごとに振り分けながら残高を積み上げる。最後に「封筒保管金（プール現金）」「現在の金額（金庫内残高）」の2つの金種表（10000円〜1円の枚数を手入力すると金額が自動計算される空テンプレート、`SUM`数式付き）を配置
- **`会計報告書を生成する」ボタンを1回押すだけで、上記2シートが同じファイルに両方作られる**（ボタンは分かれていない）
- 期末残高記録シートを新設（`recordPeriodEndBalance`）。会計報告書を生成するたびにその期の残額を記録し、次の期の会計報告書作成画面では**前期の記録から繰越金を自動提案**する（`getSuggestedCarryover`、GET action。見つからない場合は手入力を促すメッセージにフォールバック）。GASが生成したファイルの体裁を毎回パースする方式は壊れやすいためこの専用シート方式にした
- `index.html`: `report-carryover`入力欄に自動提案値をプリフィル（`report-carryover-hint`に提案元の期を表示、手動修正も可能）

**反映済み:** `clasp push`+デプロイ（GAS @39まで）、`git push`（GitHub Pages）完了。実データで動作確認済み（テスト生成した出納帳シートの残高・カテゴリ別集計・合計行がすべて期待値と一致）。テスト生成した「会計報告書_2026年度前期」ファイルは削除依頼済み（Driveのフォルダ権限周りで一部GAS関数がAccess deniedになったため、最終確認はユーザー側の手動削除に委ねた）。

**今後の課題:**
- 金種表は実カウント値の手入力のまま（自動化不可）。封筒保管金と現在の金額の位置づけの違い（プール現金 vs 金庫内残高）は今回ユーザーから確認済み
- 過去の期（2023〜2025年度）の期末残高記録は存在しないため、それらの期を今から会計報告書生成しても繰越金の自動提案は効かない（手入力にフォールバックする）

### 集会所使用料 領収書の自動生成（2026-09-26）

**経緯:** 残課題「領収書発行は現状手動運用のまま」のうち、集会所使用料分について着手。旧環境の実際の領収書ファイル（`友愛クラブ 領収書`スプレッドシート）を確認し、集会所を2ヶ月ずつ交代で使う3団体分の領収書を期末にまとめて発行する運用と分かった。団体のローテーション順は固定で「西淡路日之出友愛クラブ → 西淡路新大阪第１友愛クラブ → 西淡路新大阪第２友愛クラブ」（ユーザー確認済み）。

**実装内容:**
- `gas/コード.js`: `HALL_RECEIPT_TEMPLATE_ID`（元の領収書スプレッドシートをテンプレートとしてそのまま使用）と`HALL_CLUB_ROTATION`定数を追加
- `createHallUsageReceipts(year, half)`（新規, POST）: テンプレートを丸ごと複製し、3団体ブロック（各15行間隔、行オフセット+2=宛名/+5=但し書き/+6=日付）の宛名・但し書きの対象月・日付だけを書き換える。金額（¥18,000固定）や書式はテンプレートのまま流用
  - 日付は3枚とも「期末月の第4木曜日」で統一（`getNthThursday`を流用）
- `index.html`: メニューに「集会所使用料領収書作成」を追加。年度/期選択・3枚のプレビュー・生成後のスプレッドシートリンク表示という、集金日程ドキュメント作成と同じUIパターン

**反映済み:** `clasp push`+デプロイ（GAS @43まで）、`git push`（GitHub Pages）完了。実データで動作確認済み（2026年度前期でテスト生成し、3ブロックすべての宛名・月・日付が正しく書き込まれていることをセル単位で確認）。テスト生成した「集会所使用料領収書_2026年度前期」ファイルは自動作成ドキュメントフォルダに残っているため、不要であればユーザー側で削除。

**今後の課題:**
- 自治会費（各世帯分）の領収書発行は未着手（今回は集会所使用料分のみ）
- 金額（¥18,000/2ヶ月）が将来変わった場合はテンプレート側の金額セルを手動更新するか、コードに定数化する必要がある

### 会計報告書・集金日程ドキュメント・集会所使用料領収書にPDFダウンロードを追加（2026-09-26）

**内容:** 上記3つの自動生成機能に「PDFでダウンロード（印刷用）」リンクを追加。
- `gas/コード.js`: `sheetsPdfExportUrl(id)`（スプレッドシート用、`gid`を省略して**全シート**をまとめて1つのPDFにする）・`docPdfExportUrl(id)`（ドキュメント用）のヘルパーを追加し、`createAccountingReport`・`createCollectionDoc`・`createHallUsageReceipts`の戻り値に`pdfUrl`を追加
- 「`gid`省略で全シートPDF化される」という挙動は、既存の2シート構成の参考ファイルに対して`UrlFetchApp`でPDFサイズを比較し実証済み（単体シートの合計サイズ ≈ 省略時のサイズ）
- ついでに集金日程ドキュメントのリンク文言の誤り（実際はGoogleドキュメント/docxなのに「スプレッドシート」「Excel」と表示されていた）も修正
- `index.html`: 3画面それぞれの生成結果に🖨️PDFリンクを追加

**反映済み:** `clasp push`+デプロイ（GAS @46まで）、`git push`（GitHub Pages）完了。

### [解決済み] 月次自動バックアップがGASプロジェクトごと複製する不具合の修正（2026-09-26）

**経緯:** ユーザーが月初に手動でスプレッドシートをDrive上で複製したところ、紐付いているコンテナバインドスクリプト（このGASプロジェクト自体）まで複製されてしまい、複製されたGASの名称を手動変更する羽目になった。実は`backupSpreadsheet`（月次バックアップ用、既存実装）が`ssFile.makeCopy()`（ファイルまるごとコピー）を使っており、自動実行でも同じ問題が起きる状態だった。

**修正内容:**
- `gas/コード.js` の `backupSpreadsheet`: `SpreadsheetApp.create()`で紐付きスクリプトのない新規スプレッドシートを作り、元の各シートを`sheet.copyTo(backupSs)`でシート単位にコピーする方式に変更（GASプロジェクトは複製されない）。自動生成される初期シート（「シート1」/「Sheet1」）は削除
- `installMonthlyBackupTrigger`: 二重登録防止（既存トリガーがあれば`alreadyInstalled:true`を返して何もしない）、`SpreadsheetApp.getUi().alert()`（Apps Scriptエディタから実行するとエラーになる）を削除し戻り値ベースに変更
- `appsscript.json`: `ScriptApp.getProjectTriggers`/`newTrigger`に必要な`https://www.googleapis.com/auth/script.scriptapp`スコープを追加（追加前は「Specified permissions are not sufficient」エラーで`installMonthlyBackupTrigger`が実行できなかった）

**反映済み:** `clasp push --force`+デプロイ（GAS @51まで、appsscript.jsonの変更が反映されなかったため`--force`が必要だった）。`backupSpreadsheet`を実際に実行し、バックアップ先に9シート全て正しい名前・行数でコピーされていることを確認済み。`installMonthlyBackupTrigger`も実行済み（`alreadyInstalled:false`＝今回新規に有効化）。毎月1日AM3時に自動実行される。

**引き継ぎ時の注意:** 旧環境で手動作成された重複バックアップ（複製されたGASプロジェクト「自治会集金管理-GAS backup」、スプレッドシート「自治会集金管理_backup20260924」）はユーザー側で削除依頼済み。
## バグ対応履歴

### [解決済み] 滞納分/当期分/来期分の判定・集計方式の全面修正（2026-09-26、ClaudeCode移行後）

**症状:** 出納連携画面で「滞納が多い」と感じ確認したところ、判定・集計に複数のバグが重なっていた。

**原因（3つ、根が深い順）:**
1. **年月セルのDate型自動変換によるNaN化（最重要）**: 受領ログのC列（年月）に`"2026/09"`のような文字列を書き込むと、Googleスプレッドシート側が自動的に日付型（Dateオブジェクト）に変換してしまう。`syncLedgerFromLog`・`fixReceiptLogTypes`は`String(値).split('/')`で年月をパースしていたが、Dateオブジェクトの`String()`は`"Fri Aug 01 2025..."`のような形式になり`/`を含まないため分割に失敗、`year=NaN`になっていた。これにより判定が常に同じ結果に落ち、実行前の検証で発覚（実行していれば全件が壊れた1行にまとめて`収支記録`に登録されていたはず）。
2. **滞納分/当期分/来期分の判定基準が「今日の日付」だった**: `receiveMonth`（`index.html`）が対象月を「今日」と比較しており、期の境界（4月/10月）を見ていなかった。同一期内の後日まとめ受領が誤って滞納分になっていた。
3. **滞納分・来期分の集計基準**: 当初「対象月が属する期」で`収支記録`に振り分けていたが、運用上は「集金した期（＝反映を実行した瞬間の実際の今期）」にまとめたいとの要望があり、`syncLedgerFromLog`を作り直した。

**修正内容:**
- `gas/コード.js`:
  - `parseYmCell(val)` を新設。Date型・文字列どちらの年月セルも正しく`{year, month}`に変換する共通ヘルパー（`syncLedgerFromLog`・`fixReceiptLogTypes`・`addReceiptLogs`の重複判定で使用）
  - `getLedgerLogSummary`: `滞納分`/`当期分`/`来期分`の3分類に対応（`nextTotal`/`nextCount`を追加）
  - `syncLedgerFromLog`: 当期分は対象月自身が属する期に「自治会費」として計上。**滞納分・来期分は対象月に関わらず常に反映実行時点の実際の今期にまとめて計上**（来期分は自治会費と別項目「来期分（Nヶ月分）」として記載）
  - `fixReceiptLogTypes`（一回限り／手動実行）: 受領ログの`種別`を実際の今期の期間基準で再判定
  - `createAccountingReport`: 収入内訳に「来期分（前受金）」カテゴリを追加（`nextTermTotal`）
  - `addManualIncomeJun23`（一回限り／手動実行）: 6/23までの手入力（振込等）分193ヶ月分・¥482,500を2026年度前期の収入として追加
  - `fixTodaysLedgerSyncRows`（一回限り／手動実行、実行済み）: 上記バグ修正前に反映済みだった8行を、今期にまとめた2行に書き換え
  - GAS通信のコールバック名衝突バグも発見・修正: `index.html`の`gasGet`/`gasPost`が`Date.now()`のみでコールバック名を生成しており、同一ミリ秒内に複数回通信するとホーム画面の初期読み込み（3並列fetch）が永久にハングすることがあった。連番(`gasCallSeq`)を付与して解消
- `index.html`: `receiveMonth`の判定基準を実際の今期（`TERMS[CURRENT_IDX]`）基準に修正。出納連携画面・会計報告書プレビューに「来期分」の行を追加

**反映済み:** `clasp push`+デプロイ（GAS @30まで進行）、`git push`（GitHub Pages、複数コミット）ともに完了。実データで検証済み（204件→滞納79件¥197,500・当期106件¥265,000・来期19件¥47,500に正しく分類、収支記録へも正しく反映されたことを確認）。

**副次的に修正した引継ぎ時からの既知バグ（旧アカウントの引継ぎメモに記載があったもの）:**
- 次回集金日バナー・集金日程プレビューのフロア案内（1〜5階/6〜11階）が奇数/偶数で逆だった（GAS側`createCollectionDoc`は期の何ヶ月目か＝indexで判定、フロントは月の奇数/偶数で判定しておりズレていた）
- `getPassword`通信失敗時にPIN認証を丸ごとスキップしてメイン画面に入れてしまう問題（オンラインなのに無認証で入れる状態になり得た）
- 滞納通知書プレビューが`★振込★`/`★その他★`を除外しておらず、GAS側の実際の生成件数とズレていた

**今後の確認事項:** 2026年度前期の収支記録には、今回反映するまで収入が1件も記録されていなかった（振込払い世帯の分が別経路で管理されている可能性が高い）。実際の通帳残高・前期繰越金と突き合わせて確認すること。

### [解決済み] 出納連携→会計報告書の収入集計漏れ（2026-09-26）

**症状:** 出納連携処理で「滞納回収分」をまとめて収支記録に反映すると、会計報告書作成時にその金額が収入合計から漏れる。

**原因:** `gas/コード.js` の `createAccountingReport` 関数が、収入を「自治会費」「集会所」という文字列を含む項目名でしか集計しておらず、出納連携（`index.html` の `doLedgerSync`）が書き込む「滞納回収（Nヶ月分）」という項目名がどちらにも一致せず集計から漏れていた。

**修正内容:**
- `gas/コード.js`: `arrearsTotal`（滞納回収分）と `otherIncomeTotal`（想定外項目の安全弁）を追加し、`incomeTotal` の計算・収入内訳シート・収支まとめ・フロントエンドへの `summary` レスポンスに反映
- `index.html`: 会計報告書メニューの生成前プレビュー（`loadReportPreview`）と生成後サマリー（`submitCreateReport`）にも滞納回収分・その他収入の行を追加

**反映済み:** `clasp push`（GAS）、`git push`（GitHub Pages）ともに完了（コミット `1a33403`）。

**今後の確認事項:** 実際に滞納回収を含む出納連携→会計報告書生成を行い、金額が正しく表示されるか実データで検証する。

### [解決済み] 出納連携がローカルデータ依存で機能していなかった問題（2026-09-26）

**症状:** `受領ログ`シートには過去の集金記録が残っているのに、`収支記録`には反映されていなかった。

**原因（2つ）:**
1. 出納連携画面（`index.html`の`doLedgerSync`/`initLedger`）が、サーバー側の`受領ログ`ではなく**ブラウザのローカル(IndexedDB)にしかない`pendingLedger`変数**を見ていた。通常の「同期」ボタン（`syncToGAS`）を押すと、受領データを`受領ログ`に送信した直後に`pendingLedger`をクリアしてしまうため、出納連携で`収支記録`に反映する前にデータが消えていた。別端末に引き継ぐとローカルデータも空になるため、今回のように全く反映できなくなった。
2. `gasPost`（JSONP方式、15秒タイムアウト）は、GAS側の処理が遅いだけで実際は成功していても、クライアント側がタイムアウトと判断して失敗扱いにする。これにより「同期失敗→再実行」を繰り返すと`受領ログ`に同じ内容が何度も重複登録されていた（実績: 245行中41行が重複、¥102,500相当の水増し）。

**修正内容:**
- `gas/コード.js`:
  - `受領ログ`シートに「反映済」列を追加
  - `addReceiptLogs`: 部屋番号+年月+種別が既存と重複する場合はスキップするよう変更（再送による二重登録を防止）
  - `getLedgerLogSummary`（新規, GET）: `受領ログ`の未反映分をサーバー側で集計し画面に返す
  - `syncLedgerFromLog`（新規, POST）: 未反映分を年度・期・種別ごとに集計して`収支記録`へ反映し、対象行に「反映済」を立てる。重複行は集計に含めず反映済みのみ立てる
  - `dedupeReceiptLog`（新規, POST／一回限りの手動実行想定）: `受領ログ`内の完全重複行を削除。**2026-09-26に実行済み、41件削除済み**
- `index.html`: `initLedger`/`doLedgerSync`を上記の新しいサーバー側APIを呼ぶ形に全面書き換え。ローカルの`pendingLedger`に依存しなくなったため、どの端末からでも同じ結果になる

**反映済み:** `clasp push`+デプロイ（GAS）、`git push`（GitHub Pages, コミット`a64cf75`）ともに完了。

**今後の確認事項:**
- 出納連携画面を開き、`受領ログ`の未反映分（重複除去後で204件）が正しく集計表示されるか確認
- 「収支記録に反映する」を実行し、複数の年度・期にまたがる形で`収支記録`に正しく登録されるか確認（過去分も含めて一括反映される想定）
- 反映後、該当年度の会計報告書で収入が正しく表示されるか確認

### 環境面の注意（今後の作業について）

2026-09-26に、このPC上で直接動くClaude Codeへの引継ぎが完了。`clasp push`/`clasp deploy`ともこのPCから正常に動作することを確認済み（node/clasp本体の場所は上記「環境依存の定数」を参照）。本番データを書き換えるGAS関数（`fixReceiptLogTypes`等の一回限りの補正関数）は、Claude Codeからブラウザ経由で直接実行しようとすると安全装置でブロックされるため、Apps Scriptエディタでユーザー自身が実行する運用になっている。

## 残っている課題・積み残し

- ~~期表示バーへの階数ナビ追加~~ → **2026-09-26確認：すでに実装・動作済み**（[index.html:427-433](index.html:427) の`‹ 全フロア ›`バー、`FLOORS`配列・`changeFloor()`。実機能で1F〜11Fの切り替え・部屋別月次グリッド表示を確認済み）。このリストの記載が古いままだったための誤記載
- ~~財務要約レポート機能（戦略未定）~~ → **2026-09-26実装済み**：会計報告書生成時に「出納帳」シートを自動生成する形で対応（上記「機能追加履歴」参照）。金種表の実カウント入力・過去の期の繰越金手入力は引き続き手動
- ~~領収書発行は現状手動運用のまま（自動化は未着手）~~ → **2026-09-26：集会所使用料分は実装済み**（上記「機能追加履歴」参照）。自治会費（各世帯分）の領収書発行は引き続き未着手
- `CURRENT_IDX`のハードコード（`index.html`の`TERMS`配列内、`year===2026 && half==='前期'`と決め打ち）: 期が変わるたびに手動でコード書き換え・再デプロイが必要。階数ナビ対応とあわせて自動化を検討の余地あり
- 定例出金登録・出金登録履歴取得で、テザリング環境の通信瞬断により間欠的に「取得に失敗しました」が発生することがある（F5で復旧）。1回だけ自動リトライする案は未実装
- 集計ドキュメント・会計報告書パイプラインの再設計（旧環境での検討事項、設計合意はあるが実装未着手）: GASで収支記録から`収入`/`支出`シートを自動生成→会計担当が手動調整→調整済みシートを`createAccountingReport`の入力にする、という多段構成の案。優先度は保留
- 収支記録・受領ログのアーカイブ設計（期が閉じたら別シート/別スプレッドシートに退避する案。現状の規模では実用上の速度問題はまだ出ていない）

## システム構成・アーキテクチャ

- **フロントエンド:** GitHub Pagesで公開。JSONPでCORSを回避。IndexedDB + LocalStorageでキャッシュ。
- **バックエンド:** GAS（Google Apps Script）
- **DB:** Googleスプレッドシート。運用系シート3枚 + パスワード用シート1枚の構成。
- **認証:** PIN認証方式
- **主な機能:**
  - 階数ナビゲーション（フロア切り替え）
  - 経費登録
  - 未払い（滞納）通知PDF生成（個別 + 一括統合）。Google Docsを中間テンプレートとして利用
  - 過去フォルダのクリーンアップ処理
  - 会計報告書生成
  - 集金スケジュール文書の自動生成
- **データ状況:** 103世帯分の居住者データ、既存の滞納記録38件を移行済み
- **未払い管理:** From/To の日付レンジで管理。一括領収書発行のロジックあり。

## 環境依存の定数（旧環境から要取得・要記入）

- [x] GASデプロイURL: `https://script.google.com/macros/s/AKfycby-8TA4q_Q3ZWC-beebxArC1dyj7D5Mps84XgnMbX0Y-P4gTR4Z0V00r55Abhcc03Nk/exec`（`index.html` 内の `GAS_URL` から確認）
- [x] スプレッドシートID: `1NPEDB9KutaQba71O8KDPhittAukb9jHdtNubWy40jTw`（`gas/コード.js` の `SHEET_ID`）
- [x] 滞納通知書テンプレートID（Google Docs）: `1bPsHEe-OphHlYHLeaz_nMYAATm-OgGn5VW9OpxbZDRU`
- [x] `.clasp.json` の内容: `scriptId: "1e3qOl2Wn7i92VYfeuBi4JE3915gAc9_HrCz5-TpcOXH-IB0xzHHpNHjh"`。このPCには `gas/.clasp.json` として既に存在し、`clasp push`/`clasp deploy` とも動作確認済み（node/npmは `C:\Program Files\nodejs`、clasp本体は `%APPDATA%\npm` にあり、PATHに手動追加が必要な場合がある）
- [x] 月額費: `MONTHLY_FEE = 2500`
- [x] 除外部屋番号: `SKIP_ROOMS = [105, 106]`
- [x] 階数配列: `FLOORS = [null, 1, 2, ..., 11]`（実装済み）

**注記:** `gas/` ディレクトリ（`コード.js`・`appsscript.json`・`.clasp.json`）は現在このPC上にローカルで存在するが、**2026-09-26時点でまだgit未追跡（untracked）**。GitHubリポジトリには `index.html` と `README.md` のみがコミットされている。

## アクセス権限（引継ぎ時に要確認）

- [ ] GASプロジェクトの編集権限
- [ ] スプレッドシートの編集権限（もしくは所有者移管）
- [ ] GitHubリポジトリへのアクセス権
- [ ] GitHub Pagesのデプロイ設定へのアクセス
- [ ] GCPプロジェクトの権限（紐付けている場合）

## 運用メモ

- 集金用の端末はChromebook Duet（Gen9）。ポートレートモードで運用、スマホテザリングで利用。
- Duetはメモリ・ストレージが控えめなため、開発作業（Claude Code含む）は別端末で行い、Duetには完成物だけデプロイする分業を想定。
